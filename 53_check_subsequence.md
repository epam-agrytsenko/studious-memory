##### Check Subsequence

Write a function that determines if one string is a subsequence of another. A subsequence maintains the order but not necessarily consecutive.

Example:

```python
def is_subsequence(s1, s2):
    return # True or False
```

#### <u>Check yourself</u>:
```python
is_subsequence("abc", "ahbgdc") == True
is_subsequence("axc", "ahbgdc") == False
```